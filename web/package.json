{
  "name": "ember-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:demo": "VITE_DEMO_MODE=1 vite",
    "build": "vite build",
    "build:demo": "VITE_DEMO_MODE=1 VITE_DEMO_BASE=/demo/ vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "test": "vitest",
    "test:run": "vitest run",
    "e2e": "playwright test",
    "e2e:install": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.61.1",
    "@sveltejs/vite-plugin-svelte": "^7.2.0",
    "@testing-library/jest-dom": "^7.0.0",
    "@testing-library/svelte": "^5.4.2",
    "@types/node": "^26.1.1",
    "jsdom": "^29.1.1",
    "svelte": "^5.56.7",
    "svelte-check": "^4.7.3",
    "_comment_typescript": "HELD AT 6.x DELIBERATELY. svelte-check 4.7.x crashes on TypeScript 7 (TypeError: Cannot read properties of undefined reading useCaseSensitiveFileNames) because the TS 7 API drops what it reads at startup. Dependabot groups TS with the other web-deps, so a grouped bump will keep proposing 7.x - hold it back until svelte-check supports TS 7, then bump both together.",
    "typescript": "^6.0.3",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  }
}
