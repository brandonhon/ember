import{c as t,Q as o,j as a,m as n}from"./chunks/framework.BBA2dIOt.js";const g=JSON.parse('{"title":"Ember Security Findings Tracker","description":"","frontmatter":{},"headers":[],"relativePath":"SECURITY_FINDINGS.md","filePath":"SECURITY_FINDINGS.md","lastUpdated":1780802238000}'),d={name:"SECURITY_FINDINGS.md"};function r(s,e,i,c,l,p){return o(),a("div",null,[...e[0]||(e[0]=[n(`<h1 id="ember-security-findings-tracker" tabindex="-1">Ember Security Findings Tracker <a class="header-anchor" href="#ember-security-findings-tracker" aria-label="Permalink to &quot;Ember Security Findings Tracker&quot;">​</a></h1><p><strong>Audit date:</strong> 2026-06-06<br><strong>Frameworks:</strong> MITRE ATT&amp;CK v14 · NIST CSF 2.0 · MITRE ATLAS · MITRE D3FEND · NIST AI RMF<br><strong>Status key:</strong> <code>open</code> · <code>in_progress</code> · <code>fixed</code> · <code>wont_fix</code> · <code>accepted_risk</code></p><hr><h2 id="critical-2" tabindex="-1">CRITICAL (2) <a class="header-anchor" href="#critical-2" aria-label="Permalink to &quot;CRITICAL (2)&quot;">​</a></h2><h3 id="c-1-—-dns-rebinding-toctou-discovery-client-missing-guardedtransport" tabindex="-1">C-1 — DNS-Rebinding TOCTOU: Discovery Client Missing <code>GuardedTransport</code> <a class="header-anchor" href="#c-1-—-dns-rebinding-toctou-discovery-client-missing-guardedtransport" aria-label="Permalink to &quot;C-1 — DNS-Rebinding TOCTOU: Discovery Client Missing \`GuardedTransport\`&quot;">​</a></h3><p><strong>Status:</strong> <code>fixed</code><br><strong>File:</strong> <code>internal/api/feed_handlers.go:63–68, 124–128</code><br><strong>ATT&amp;CK:</strong> T1090.003 | <strong>NIST CSF:</strong> PR.DS-5, ID.RA-1 | <strong>D3FEND:</strong> D3-NTA, D3-OAM</p><p>Both <code>handleAddFeed</code> and <code>handleDiscoverFeeds</code> build an <code>http.Client</code> with only <code>CheckRedirect</code> set, using <code>http.DefaultTransport</code>. <code>urlcheck.Check</code> resolves the hostname at pre-flight, but the standard transport re-resolves independently at dial time. An attacker who controls DNS can return a public IP to the pre-flight and <code>169.254.169.254</code> (or any private address) to the actual dial — bypassing the SSRF guard entirely.</p><p>The poller path at <code>poller.go:528</code> correctly uses <code>urlcheck.GuardedTransport</code>. The fix is identical:</p><div class="language-go vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">go</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">disco </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">http</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Client</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Timeout:       </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> time.Second,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Transport:     urlcheck.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">GuardedTransport</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(d.AllowPrivateURLs),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    CheckRedirect: feed.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">RedirectGuard</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><hr><h3 id="c-2-—-content-type-confusion-text-xml-application-xml-accepted-as-feed-→-stored-xss-surface" tabindex="-1">C-2 — Content-Type Confusion: <code>text/xml</code>/<code>application/xml</code> Accepted as Feed → Stored XSS Surface <a class="header-anchor" href="#c-2-—-content-type-confusion-text-xml-application-xml-accepted-as-feed-→-stored-xss-surface" aria-label="Permalink to &quot;C-2 — Content-Type Confusion: \`text/xml\`/\`application/xml\` Accepted as Feed → Stored XSS Surface&quot;">​</a></h3><p><strong>Status:</strong> <code>fixed</code><br><strong>File:</strong> <code>internal/feed/discover.go:244–251</code><br><strong>ATT&amp;CK:</strong> T1059.007, T1190 | <strong>NIST CSF:</strong> PR.DS-2, PR.IP-1 | <strong>D3FEND:</strong> D3-SAOR, D3-SAN</p><p><code>isFeedContentType</code> accepts <code>application/xml</code> and <code>text/xml</code>, which also match SVG, XHTML, SOAP, and arbitrary XML APIs. A malicious site serves crafted XML under <code>text/xml</code> that <code>gofeed</code> parses with attacker-controlled <code>Title</code>, <code>SiteURL</code>, <code>Author</code>, and <code>Tags</code> fields — none of which are sanitized before storage (only <code>ContentHTML</code> goes through bluemonday).</p><p>Fix: Remove <code>application/xml</code> and <code>text/xml</code> from <code>isFeedContentType</code>; keep only <code>application/rss+xml</code>, <code>application/atom+xml</code>, and <code>application/feed+json</code>.</p><hr><h2 id="high-13" tabindex="-1">HIGH (13) <a class="header-anchor" href="#high-13" aria-label="Permalink to &quot;HIGH (13)&quot;">​</a></h2><h3 id="h-1-—-prompt-injection-via-feed-content-into-ollama-llm" tabindex="-1">H-1 — Prompt Injection via Feed Content into Ollama LLM <a class="header-anchor" href="#h-1-—-prompt-injection-via-feed-content-into-ollama-llm" aria-label="Permalink to &quot;H-1 — Prompt Injection via Feed Content into Ollama LLM&quot;">​</a></h3><p><strong>Status:</strong> <code>fixed</code><br><strong>File:</strong> <code>internal/summarize/ollama.go:242–245</code><br><strong>ATT&amp;CK:</strong> T1059.007 | <strong>ATLAS:</strong> AML.T0051, AML.T0048 | <strong>NIST CSF:</strong> GV.SC-07, PR.DS-02, DE.CM-09 | <strong>D3FEND:</strong> D3-IBAR, D3-OAM | <strong>AI RMF:</strong> GOVERN 1.2, MAP 1.5, MEASURE 2.5</p><p>Article <code>title</code> and <code>text</code> are inserted directly into <code>promptTemplate</code> via <code>fmt.Sprintf</code> with no structural separation. An attacker who controls a feed can craft content with embedded LLM instructions. Because the CLEANED section output is stored and rendered by the SPA, a successful injection can escalate to stored XSS even though bluemonday runs afterward — the attacker controls the model&#39;s output, not the sanitization input.</p><p>Fix:</p><ol><li>Inject article content via an XML wrapper the prompt designates as inviolable (e.g. <code>&lt;article&gt;…&lt;/article&gt;</code>).</li><li>Before paragraphizing model output: if <code>res.Cleaned</code> contains <code>&lt;</code>, apply <code>html.EscapeString</code> to the entire string — treat model output as plaintext unconditionally.</li></ol><hr><h3 id="h-2-—-tt-rss-live-pull-api-client-missing-guardedtransport-credential-exfil" tabindex="-1">H-2 — TT-RSS Live-Pull API Client Missing <code>GuardedTransport</code> + Credential Exfil <a class="header-anchor" href="#h-2-—-tt-rss-live-pull-api-client-missing-guardedtransport-credential-exfil" aria-label="Permalink to &quot;H-2 — TT-RSS Live-Pull API Client Missing \`GuardedTransport\` + Credential Exfil&quot;">​</a></h3><p><strong>Status:</strong> <code>fixed</code><br><strong>File:</strong> <code>internal/ttrss/api.go:100–111</code><br><strong>ATT&amp;CK:</strong> T1190, T1078 | <strong>NIST CSF:</strong> PR.DS-5, PR.AC-3 | <strong>D3FEND:</strong> D3-NTA, D3-OAM</p><p><code>apiClient()</code> uses <code>http.DefaultTransport</code> in production — same DNS-rebinding TOCTOU as C-1. Critically, the TT-RSS import sends a plaintext <code>password</code> field to the resolved address, making this a credential-exfiltration vector in addition to SSRF.</p><p>Fix: <code>Transport: urlcheck.GuardedTransport(false)</code> (or thread <code>allowPrivate</code> through <code>Service</code>) in <code>apiClient</code>.</p><hr><h3 id="h-3-—-ssrf-via-aggregator-feed-body-external-links" tabindex="-1">H-3 — SSRF via Aggregator Feed Body External Links <a class="header-anchor" href="#h-3-—-ssrf-via-aggregator-feed-body-external-links" aria-label="Permalink to &quot;H-3 — SSRF via Aggregator Feed Body External Links&quot;">​</a></h3><p><strong>Status:</strong> <code>fixed</code><br><strong>File:</strong> <code>internal/poller/poller.go:501–519</code><br><strong>ATT&amp;CK:</strong> T1090.001, T1210 | <strong>NIST CSF:</strong> PR.PS-05, PR.IR-01 | <strong>D3FEND:</strong> D3-UA, D3-NTF</p><p><code>enrichWithReadability</code> extracts URLs from raw feed HTML using a regex and fetches them for readability processing. Feed publishers control these URLs. When <code>AllowPrivateURLs=true</code> (common homelab config) the <code>urlcheck</code> guard and <code>GuardedTransport</code> are disabled — an attacker-controlled aggregator feed causes Ember to fetch any LAN address.</p><p>Fix: Replace <code>hrefRE</code> with <code>golang.org/x/net/html</code> tokenizer for href extraction; add WARN-level logging when aggregator fallback triggers.</p><hr><h3 id="h-4-—-tt-rss-ssrf-guard-nil-default-is-unsafe" tabindex="-1">H-4 — TT-RSS SSRF Guard Nil-Default Is Unsafe <a class="header-anchor" href="#h-4-—-tt-rss-ssrf-guard-nil-default-is-unsafe" aria-label="Permalink to &quot;H-4 — TT-RSS SSRF Guard Nil-Default Is Unsafe&quot;">​</a></h3><p><strong>Status:</strong> <code>fixed</code><br><strong>File:</strong> <code>internal/ttrss/ttrss.go:52–55</code>, <code>internal/ttrss/api.go:97–110</code><br><strong>ATT&amp;CK:</strong> T1090.001, T1602 | <strong>NIST CSF:</strong> PR.PS-05, ID.RA-01 | <strong>D3FEND:</strong> D3-NTF, D3-UA</p><p><code>Service.ValidateURL</code> is nil by default, which disables the redirect guard entirely. The production wiring sets it, but the type&#39;s zero-value is unsafe-by-default. A future caller constructing <code>&amp;ttrss.Service{Store: s}</code> without setting <code>ValidateURL</code> would have no SSRF guard.</p><p>Fix: Invert the default — make SSRF blocking unconditional; add an explicit <code>AllowAllURLs bool</code> opt-in instead of nil-means-skip.</p><hr><h3 id="h-5-—-tt-rss-h-link-not-sanitized-via-safehttpurl-→-javascript-url-stored" tabindex="-1">H-5 — TT-RSS <code>h.Link</code> Not Sanitized via <code>SafeHTTPURL</code> → <code>javascript:</code> URL Stored <a class="header-anchor" href="#h-5-—-tt-rss-h-link-not-sanitized-via-safehttpurl-→-javascript-url-stored" aria-label="Permalink to &quot;H-5 — TT-RSS \`h.Link\` Not Sanitized via \`SafeHTTPURL\` → \`javascript:\` URL Stored&quot;">​</a></h3><p><strong>Status:</strong> <code>fixed</code><br><strong>File:</strong> <code>internal/ttrss/api.go:126–133</code><br><strong>ATT&amp;CK:</strong> T1059.007 | <strong>NIST CSF:</strong> PR.DS-2, PR.IP-1 | <strong>D3FEND:</strong> D3-SAN</p><p>The live TT-RSS API pull path stores <code>h.Link</code> verbatim without calling <code>feed.SafeHTTPURL</code>. A TT-RSS server returning a <code>javascript:</code> or <code>data:</code> scheme in the link field stores it as an <code>&lt;a href&gt;</code> rendered by the SPA.</p><p>Fix: <code>link: feed.SafeHTTPURL(h.Link)</code> in the <code>normItem</code> construction.</p><hr><h3 id="h-6-—-urlcheck-error-strings-returned-to-clients-—-internal-ip-oracle" tabindex="-1">H-6 — <code>urlcheck</code> Error Strings Returned to Clients — Internal IP Oracle <a class="header-anchor" href="#h-6-—-urlcheck-error-strings-returned-to-clients-—-internal-ip-oracle" aria-label="Permalink to &quot;H-6 — \`urlcheck\` Error Strings Returned to Clients — Internal IP Oracle&quot;">​</a></h3><p><strong>Status:</strong> <code>fixed</code><br><strong>File:</strong> <code>internal/api/feed_handlers.go:54, 74, 119, 135</code><br><strong>ATT&amp;CK:</strong> T1590, T1046 | <strong>NIST CSF:</strong> PR.DS-5, DE.CM-1 | <strong>D3FEND:</strong> D3-EHB</p><p><code>err.Error()</code> from <code>urlcheck.Check</code> (which includes resolved IPs like <code>&quot;urlcheck: URL resolves to private address: 10.0.0.5&quot;</code>) is forwarded verbatim to the API response. Any authenticated user can enumerate private IP space by observing rejection messages.</p><p>Fix: Return <code>&quot;URL is not allowed&quot;</code> to the client; log the full error server-side.</p><hr><h3 id="h-7-—-ollama-baseurl-not-ssrf-validated" tabindex="-1">H-7 — Ollama BaseURL Not SSRF-Validated <a class="header-anchor" href="#h-7-—-ollama-baseurl-not-ssrf-validated" aria-label="Permalink to &quot;H-7 — Ollama BaseURL Not SSRF-Validated&quot;">​</a></h3><p><strong>Status:</strong> <code>fixed</code><br><strong>File:</strong> <code>internal/config/config.go:117–119</code>, <code>internal/summarize/ollama.go:42–51</code><br><strong>ATT&amp;CK:</strong> T1090.002, T1602 | <strong>NIST CSF:</strong> PR.AA-05, ID.RA-01 | <strong>D3FEND:</strong> D3-NTF, D3-UA</p><p><code>EMBER_OLLAMA_URL</code> is stored verbatim. An admin or compromised config can point it at <code>http://169.254.169.254/</code> or <code>http://127.0.0.1:2375/</code> (Docker API). All pull/generate/delete/list operations hit that address.</p><p>Fix: At config load time, parse the URL and validate the scheme is <code>http</code>/<code>https</code>. For non-homelab deployments, apply the same private-IP block as <code>urlcheck</code> unless <code>AllowPrivateURLs</code> is set.</p><hr><h3 id="h-8-—-model-pull-endpoint-30-minute-blocking-call-no-concurrency-guard" tabindex="-1">H-8 — Model Pull Endpoint: 30-Minute Blocking Call, No Concurrency Guard <a class="header-anchor" href="#h-8-—-model-pull-endpoint-30-minute-blocking-call-no-concurrency-guard" aria-label="Permalink to &quot;H-8 — Model Pull Endpoint: 30-Minute Blocking Call, No Concurrency Guard&quot;">​</a></h3><p><strong>Status:</strong> <code>fixed</code><br><strong>File:</strong> <code>internal/api/llm_handlers.go:176–213</code><br><strong>ATT&amp;CK:</strong> T1499.001 | <strong>NIST CSF:</strong> PR.IR-04, DE.CM-01 | <strong>D3FEND:</strong> D3-RTSD</p><p>Each admin-triggered <code>handlePullLLMModel</code> blocks for up to 30 minutes. No mutex prevents concurrent pulls. The handler extends the connection deadline to 35 minutes, bypassing the global 60-second middleware timeout.</p><p>Fix: Atomic in-progress flag; return HTTP 409 if a pull is already running; expose status in <code>handleGetLLM</code>.</p><hr><h3 id="h-9-—-fever-authentication-uses-md5-derived-token" tabindex="-1">H-9 — Fever Authentication Uses MD5-Derived Token <a class="header-anchor" href="#h-9-—-fever-authentication-uses-md5-derived-token" aria-label="Permalink to &quot;H-9 — Fever Authentication Uses MD5-Derived Token&quot;">​</a></h3><p><strong>Status:</strong> <code>accepted_risk</code><br><strong>File:</strong> <code>internal/api/fever.go:17</code><br><strong>ATT&amp;CK:</strong> T1110.002, T1552.001 | <strong>NIST CSF:</strong> PR.AA-03, PR.DS-02 | <strong>D3FEND:</strong> D3-SPP, D3-MFA</p><p>The Fever protocol mandates <code>md5(username:password)</code> as the API key. MD5 is broken — a compromised DB token is trivially reversible offline. This cannot be fixed without breaking protocol compatibility.</p><p>Mitigations: (1) Mandate TLS at the edge when Fever is enabled. (2) Add a per-user Fever enable/disable toggle. (3) Document as accepted protocol-level risk in <code>docs/security.md</code>.</p><hr><h3 id="h-10-—-webauthn-takewebauthnsession-toctou-race" tabindex="-1">H-10 — WebAuthn <code>TakeWebAuthnSession</code> TOCTOU Race <a class="header-anchor" href="#h-10-—-webauthn-takewebauthnsession-toctou-race" aria-label="Permalink to &quot;H-10 — WebAuthn \`TakeWebAuthnSession\` TOCTOU Race&quot;">​</a></h3><p><strong>Status:</strong> <code>fixed</code><br><strong>File:</strong> <code>internal/store/passkeys.go:171</code><br><strong>ATT&amp;CK:</strong> T1550 | <strong>NIST CSF:</strong> PR.AA-05 | <strong>D3FEND:</strong> D3-OTP</p><p>SELECT then DELETE is not atomic. Two concurrent requests with the same session ID can both read the row before either DELETE fires, enabling ceremony replay. <code>DELETE … RETURNING</code> (SQLite 3.35+, supported by <code>modernc.org/sqlite</code> v1.50+) makes this atomic.</p><hr><h3 id="h-11-—-finishregister-resolves-target-user-from-session-not-auth-context" tabindex="-1">H-11 — <code>FinishRegister</code> Resolves Target User from Session, Not Auth Context <a class="header-anchor" href="#h-11-—-finishregister-resolves-target-user-from-session-not-auth-context" aria-label="Permalink to &quot;H-11 — \`FinishRegister\` Resolves Target User from Session, Not Auth Context&quot;">​</a></h3><p><strong>Status:</strong> <code>fixed</code><br><strong>File:</strong> <code>internal/auth/webauthn.go:141–188</code>, <code>internal/api/passkey_handlers.go:93–121</code><br><strong>ATT&amp;CK:</strong> T1556, T1550 | <strong>NIST CSF:</strong> PR.AA-05 | <strong>D3FEND:</strong> D3-UAM</p><p><code>handlePasskeyRegisterFinish</code> is behind <code>RequireAuth</code> but never verifies the authenticated caller&#39;s ID matches <code>sess.UserID</code>. Session ID entropy (128-bit) makes guessing infeasible, but the binding check is architecturally absent.</p><p>Fix: Pass <code>callerID</code> from the auth context into <code>FinishRegister</code> and assert <code>sess.UserID.Int64 == callerID</code>.</p><hr><h3 id="h-12-—-summary-backfill-on-restart-can-saturate-llm" tabindex="-1">H-12 — Summary Backfill on Restart Can Saturate LLM <a class="header-anchor" href="#h-12-—-summary-backfill-on-restart-can-saturate-llm" aria-label="Permalink to &quot;H-12 — Summary Backfill on Restart Can Saturate LLM&quot;">​</a></h3><p><strong>Status:</strong> <code>fixed</code><br><strong>File:</strong> <code>internal/poller/poller.go:596–615</code><br><strong>ATT&amp;CK:</strong> T1499.002 | <strong>NIST CSF:</strong> PR.IR-04, DE.CM-01 | <strong>AI RMF:</strong> GOVERN 1.2, MAP 1.5</p><p><code>enqueuePendingSummaries</code> at startup enqueues up to 256 articles. With a 90-second Ollama timeout each, this is potentially 6+ hours of sustained load against a shared Ollama instance with no rate limiting or backpressure.</p><p>Fix: Token-bucket rate limiter in the summary worker loop; log the backfill count at startup.</p><hr><h3 id="h-13-—-llm-cleaned-html-stored-without-structural-output-validation" tabindex="-1">H-13 — LLM-Cleaned HTML Stored Without Structural Output Validation <a class="header-anchor" href="#h-13-—-llm-cleaned-html-stored-without-structural-output-validation" aria-label="Permalink to &quot;H-13 — LLM-Cleaned HTML Stored Without Structural Output Validation&quot;">​</a></h3><p><strong>Status:</strong> <code>fixed</code><br><strong>File:</strong> <code>internal/poller/poller.go:653–661</code><br><strong>ATLAS:</strong> AML.T0048, AML.T0056 | <strong>AI RMF:</strong> MEASURE 2.5, MANAGE 2.4 | <strong>D3FEND:</strong> D3-OAM, D3-IBAR</p><p><code>paragraphizePlain</code> applies <code>htmlEscape</code> to individual lines but not to paragraph-split boundaries. A prompt-injected model output containing <code>&lt;/p&gt;&lt;script&gt;…&lt;/script&gt;&lt;p&gt;</code> across a chunk boundary can survive <code>htmlEscape</code> and reach bluemonday as the sole XSS defense.</p><p>Fix: If <code>res.Cleaned</code> contains <code>&lt;</code>, apply <code>html.EscapeString</code> to the entire string before paragraphizing — treat model output as plaintext unconditionally.</p><hr><h2 id="medium-16" tabindex="-1">MEDIUM (16) <a class="header-anchor" href="#medium-16" aria-label="Permalink to &quot;MEDIUM (16)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>ID</th><th>Status</th><th>File</th><th>Issue</th><th>ATT&amp;CK</th></tr></thead><tbody><tr><td>M-1</td><td><code>fixed</code></td><td><code>internal/api/render.go:96</code></td><td>JSON decode errors returned verbatim — Go struct field names leaked to unauthenticated callers. Fix: return <code>&quot;invalid request body&quot;</code>, log detail server-side.</td><td>T1592</td></tr><tr><td>M-2</td><td><code>fixed</code></td><td><code>internal/api/auth_handlers.go:159</code></td><td><code>settings_json</code> stored without <code>json.Valid()</code> check or size cap (max 64 KiB recommended).</td><td>T1565.001</td></tr><tr><td>M-3</td><td><code>fixed</code></td><td><code>internal/api/user_handlers.go:102</code></td><td>Admin <code>PATCH /api/users/{id}</code> bypasses the 8-char password minimum enforced on create/change-password paths.</td><td>T1098</td></tr><tr><td>M-4</td><td><code>fixed</code></td><td><code>internal/api/user_handlers.go:111</code></td><td>Admin can self-demote (set own <code>is_admin=false</code>) with no guard — causes permanent lockout if sole admin.</td><td>T1078.003</td></tr><tr><td>M-5</td><td><code>fixed</code></td><td><code>internal/api/middleware.go:255</code></td><td>CSRF: passkey login relies on implicit bypass (no session cookie) rather than explicit path allowlist. <code>ember_session</code> hardcoded instead of <code>auth.CookieName</code>.</td><td>T1606</td></tr><tr><td>M-6</td><td><code>fixed</code></td><td><code>internal/api/middleware.go:44–53</code></td><td>CSP: no explicit <code>script-src</code> directive; <code>data:</code> in <code>img-src</code> (tracking-pixel exfil); <code>&#39;unsafe-inline&#39;</code> in <code>style-src</code> (CSS injection).</td><td>T1059.007</td></tr><tr><td>M-7</td><td><code>fixed</code></td><td><code>internal/api/share_handlers.go:44</code></td><td>Unbounded <code>limit</code> parameter on inbox endpoint — no upper cap. Add <code>const maxInboxLimit = 200</code>.</td><td>T1499.002</td></tr><tr><td>M-8</td><td><code>fixed</code></td><td><code>internal/store/dbops.go:38</code></td><td><code>VACUUM INTO</code> uses <code>fmt.Sprintf</code> with single-quote escape — safe today since <code>dir</code> is a constant, fragile if <code>dir</code> is ever exposed to user input. Add an assertion that <code>dir</code> is server-controlled.</td><td>T1190</td></tr><tr><td>M-9</td><td><code>fixed</code></td><td><code>internal/feed/sanitize.go:17</code></td><td>bluemonday UGC policy allows <code>target=&quot;_blank&quot;</code> without requiring <code>rel=&quot;noopener noreferrer&quot;</code> — enables tab-napping.</td><td>T1185</td></tr><tr><td>M-10</td><td><code>accepted_risk</code></td><td><code>internal/ttrss/api.go:173</code></td><td>TT-RSS credentials transmitted without TLS enforcement — <code>feed.NormalizeInputURL</code> already upgrades bare <code>http://</code> input to <code>https://</code>; explicit error for intentional http:// is low-value for homelab use.</td><td>T1040, T1557</td></tr><tr><td>M-11</td><td><code>fixed</code></td><td><code>internal/digest/digest.go:294</code></td><td>MIME boundary is timestamp-derived (<code>time.Now().UnixNano()</code>), not cryptographically random. Fix: <code>&quot;ember-&quot; + hex.EncodeToString(rand16bytes)</code>.</td><td>—</td></tr><tr><td>M-12</td><td><code>fixed</code></td><td><code>internal/digest/digest.go:283</code></td><td><code>isLoopbackHost</code> checks literal string/IP only — does not resolve hostnames. A Docker service name resolving to <code>127.x</code> is not caught.</td><td>—</td></tr><tr><td>M-13</td><td><code>fixed</code></td><td><code>internal/poller/poller.go:409</code></td><td><code>hrefRE</code> regex parses raw HTML for href extraction instead of an HTML parser — bypassable with single-quoted attrs, comments, userinfo-in-host trick. Fixed as side-effect of H-3 (HTML tokenizer).</td><td>T1190</td></tr><tr><td>M-14</td><td><code>fixed</code></td><td><code>internal/summarize/noop.go</code></td><td>Noop summarizer has no guard against accidental production use — add a startup log line identifying the active Summarizer implementation.</td><td>— (AI RMF: GOVERN 1.7)</td></tr><tr><td>M-15</td><td><code>fixed</code></td><td><code>internal/ttrss/api.go:100</code></td><td>Redirect guard inherits import request context — cancellation produces misleading SSRF rejection logs. Use a separate background context with per-redirect timeout.</td><td>—</td></tr><tr><td>M-16</td><td><code>fixed</code></td><td><code>internal/api/fever.go:85–113</code></td><td>Fever items endpoint: store errors silently swallowed with <code>_, _</code>; mark errors captured and logged.</td><td>T1530</td></tr></tbody></table><hr><h2 id="low-8" tabindex="-1">LOW (8) <a class="header-anchor" href="#low-8" aria-label="Permalink to &quot;LOW (8)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>ID</th><th>Status</th><th>File</th><th>Issue</th></tr></thead><tbody><tr><td>L-1</td><td><code>fixed</code></td><td><code>internal/api/middleware.go:30</code></td><td>HSTS header omits <code>preload</code> — first-visit MITM window for public deployments. Added <code>EMBER_HSTS_PRELOAD=true</code> env var; appends <code>; preload</code> when set.</td></tr><tr><td>L-2</td><td><code>fixed</code></td><td><code>internal/api/middleware.go:255</code></td><td>CSRF exemption for passkey login is implicit (no-session bypass) rather than explicit path allowlist — fixed as part of M-5 with an explicit path list.</td></tr><tr><td>L-3</td><td><code>fixed</code></td><td><code>internal/api/fever.go:140</code></td><td><code>feverFindUser</code> does full-table scan on every Fever request. Added <code>UNIQUE INDEX ON users(fever_token)</code> (migration 0013) and <code>GetUserByFeverToken</code> store method; direct lookup replaces table scan.</td></tr><tr><td>L-4</td><td><code>fixed</code></td><td><code>internal/store/admin_settings.go:117</code></td><td>SMTP password stored plaintext in SQLite — documented in <code>docs/security.md</code> (Secrets at rest section) with filesystem permission guidance.</td></tr><tr><td>L-5</td><td><code>accepted_risk</code></td><td><code>internal/api/health.go:13</code></td><td><code>/readyz</code> returns HTTP 503 when DB is down — binary oracle. Changing to always-200 would break Docker/Kubernetes orchestrator healthchecks. Acceptable for homelab; the leaked state (DB availability) carries minimal attacker value.</td></tr><tr><td>L-6</td><td><code>fixed</code></td><td><code>cmd/ember/db_maintenance.go:93</code></td><td><code>os.Create</code> replaced with <code>os.OpenFile(..., 0o600)</code> so OPML exports are owner-readable only, not world-readable.</td></tr><tr><td>L-7</td><td><code>fixed</code></td><td><code>internal/feed/discover.go:38, 120</code></td><td><code>http.DefaultClient</code> fallback removed from <code>Discover</code>/<code>DiscoverAll</code> — both now return an error when <code>c == nil</code>.</td></tr><tr><td>L-8</td><td><code>fixed</code></td><td><code>internal/feed/discover.go:318–321</code></td><td><code>&lt;?xml</code> body sniff replaced with <code>&lt;rdf:rdf</code> — limits feed detection to RSS, Atom, and RSS 1.0/RDF only.</td></tr></tbody></table><hr><h2 id="fix-priority-order" tabindex="-1">Fix Priority Order <a class="header-anchor" href="#fix-priority-order" aria-label="Permalink to &quot;Fix Priority Order&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>IMMEDIATE — fix before next release:</span></span>
<span class="line"><span>  C-1   GuardedTransport on feed discovery HTTP client</span></span>
<span class="line"><span>  C-2   Remove text/xml and application/xml from isFeedContentType</span></span>
<span class="line"><span>  H-2   GuardedTransport on TT-RSS apiClient</span></span>
<span class="line"><span>  H-5   SafeHTTPURL on h.Link in TT-RSS live pull</span></span>
<span class="line"><span></span></span>
<span class="line"><span>HIGH — next sprint:</span></span>
<span class="line"><span>  H-1   Structural separation in Ollama prompt (prompt injection)</span></span>
<span class="line"><span>  H-13  Treat LLM CLEANED output as plaintext before paragraphizing</span></span>
<span class="line"><span>  H-3   Replace hrefRE with html.Parse in enrichWithReadability</span></span>
<span class="line"><span>  H-4   Invert TT-RSS SSRF guard to fail-safe default</span></span>
<span class="line"><span>  H-6   Redact urlcheck error strings from API responses</span></span>
<span class="line"><span>  H-7   Validate EMBER_OLLAMA_URL scheme at config load</span></span>
<span class="line"><span>  H-8   Concurrency guard on model pull endpoint (HTTP 409)</span></span>
<span class="line"><span>  H-10  Atomic TakeWebAuthnSession via DELETE...RETURNING</span></span>
<span class="line"><span>  H-11  Bind FinishRegister caller ID from auth context</span></span>
<span class="line"><span>  H-12  Rate-limit summary backfill worker</span></span>
<span class="line"><span></span></span>
<span class="line"><span>MEDIUM — hardening sprint:</span></span>
<span class="line"><span>  M-1, M-2, M-3, M-4, M-5   Input validation and admin path gaps</span></span>
<span class="line"><span>  M-6                         CSP tightening (explicit script-src, remove data: from img-src)</span></span>
<span class="line"><span>  M-7                         Inbox limit cap</span></span>
<span class="line"><span>  M-9                         bluemonday: require noopener on target=_blank</span></span>
<span class="line"><span>  M-10                        Enforce HTTPS for TT-RSS credential transmission</span></span>
<span class="line"><span>  M-16                        Fever pagination and error handling</span></span>
<span class="line"><span>  M-11, M-12, M-13, M-14, M-15   Remaining medium items</span></span>
<span class="line"><span></span></span>
<span class="line"><span>LOW — opportunistic:</span></span>
<span class="line"><span>  L-1 through L-8</span></span></code></pre></div><hr><h2 id="positive-security-baseline-no-action-needed" tabindex="-1">Positive Security Baseline (no action needed) <a class="header-anchor" href="#positive-security-baseline-no-action-needed" aria-label="Permalink to &quot;Positive Security Baseline (no action needed)&quot;">​</a></h2><ul><li><strong>argon2id</strong> — correct params (64 MiB / 3 iter / 2 parallel), <code>crypto/rand</code> salt, constant-time verify, timing equalization on missing user</li><li><strong>Session management</strong> — 256-bit random IDs, HMAC-signed gorilla cookies, <code>HttpOnly</code> + <code>SameSiteStrict</code>, server-side expiry, session rotation on login</li><li><strong>CSRF</strong> — double-submit cookie with constant-time compare; Fever shim correctly excluded</li><li><strong>Parameterized SQL everywhere</strong> — zero string concatenation in query paths; FTS5 injection handled correctly</li><li><strong>SSRF on primary poller path</strong> — <code>GuardedTransport</code> with DNS pinning, redirect guard, scheme allowlist</li><li><strong>HTML sanitization</strong> — bluemonday on all ingested content; <code>SafeHTTPURL</code> on all feed-supplied links in the main parse path</li><li><strong>SMTP hardening</strong> — CRLF-stripping on all headers, StartTLS enforced for non-loopback relays, TLS 1.2 minimum</li><li><strong>Admin route protection</strong> — all <code>/api/admin/*</code> and <code>/metrics</code> behind <code>RequireAdmin</code></li><li><strong>IDOR prevention</strong> — every store method scopes to <code>user_id</code>; no object reference leakage found</li><li><strong>Password hash never serialized</strong> — <code>PasswordHash</code> and <code>FeverToken</code> both tagged <code>json:&quot;-&quot;</code></li></ul>`,92)])])}const u=t(d,[["render",r]]);export{g as __pageData,u as default};
