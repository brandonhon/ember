import{c as o,Q as t,j as r,m as s}from"./chunks/framework.BBA2dIOt.js";const u=JSON.parse('{"title":"Security","description":"","frontmatter":{},"headers":[],"relativePath":"security.md","filePath":"security.md","lastUpdated":1780453385000}'),a={name:"security.md"};function i(d,e,n,c,l,h){return t(),r("div",null,[...e[0]||(e[0]=[s('<h1 id="security" tabindex="-1">Security <a class="header-anchor" href="#security" aria-label="Permalink to &quot;Security&quot;">​</a></h1><p>Ember has been written with the assumption that it will be exposed to the public internet behind a reverse proxy. This page lists the defenses in place and the deployment expectations.</p><p>For vulnerability reporting, see <a href="https://github.com/brandonhon/ember/blob/main/SECURITY.md" target="_blank" rel="noreferrer">SECURITY.md</a>. Don&#39;t open public issues for security problems.</p><h2 id="authentication" tabindex="-1">Authentication <a class="header-anchor" href="#authentication" aria-label="Permalink to &quot;Authentication&quot;">​</a></h2><ul><li><strong>Passwords</strong>: argon2id (<code>time=3</code>, <code>memory=64 MiB</code>, <code>parallelism=2</code>, salt=16 bytes). Meets OWASP 2024 recommendations.</li><li><strong>Sessions</strong>: 64-hex-char random IDs signed via <code>gorilla/securecookie</code>. Server-side row in <code>sessions</code> table backs every cookie. Default lifetime 24 hours; override via <code>EMBER_SESSION_TTL</code> env var or <strong>Settings → Sessions</strong> (bounded 5 min – 90 days). Destroyed on logout, login (fixation defense), and on password change (both self-service and admin).</li><li><strong>Cookies</strong>: <code>HttpOnly</code>, <code>Secure</code>, <code>SameSite=Strict</code>, scoped to <code>/</code>.</li><li><strong>Rate limiting</strong>: per-IP token bucket on <code>POST /api/auth/login</code> and <code>POST /api/auth/passkey/*</code> (burst 10/min). A second, higher-burst bucket (30/min) guards the expensive authenticated endpoints — add-feed, refresh-feed, resummarize(-all), OPML import, starter-pack import, article extract, and search — which spawn outbound fetches / goroutines / FTS work. Buckets key on the real client IP (see Transport / proxy expectations for how that&#39;s resolved).</li><li><strong>Passkeys / WebAuthn</strong>: optional second sign-in method (FIDO2). Credentials are bound to a relying-party ID derived from <code>EMBER_PUBLIC_URL</code>; ceremonies expire after 5 minutes; a stale <code>webauthn_sessions</code> row is reaped on a 15-minute cadence. Credentials never leave the device — only the public key is stored.</li></ul><h2 id="authorization" tabindex="-1">Authorization <a class="header-anchor" href="#authorization" aria-label="Permalink to &quot;Authorization&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Endpoint group</th><th>Required</th></tr></thead><tbody><tr><td><code>/healthz</code></td><td>none</td></tr><tr><td><code>POST /api/auth/login</code></td><td>none (login is the bootstrap)</td></tr><tr><td>All other <code>/api/*</code></td><td>session cookie</td></tr><tr><td><code>POST /api/users</code>, <code>PATCH /api/users/{id}</code>, admin LLM, branding, DB, settings</td><td><code>is_admin = 1</code></td></tr><tr><td><code>GET /api/admin/settings</code>, <code>PATCH /api/admin/settings</code>, <code>POST /api/admin/settings/email-test</code></td><td><code>is_admin = 1</code></td></tr><tr><td><code>/metrics</code></td><td><code>is_admin = 1</code></td></tr><tr><td><code>GET /api/users</code></td><td>returns <code>{id, username}</code> projection for non-admins</td></tr></tbody></table><p>Every user-scoped store query carries <code>WHERE user_id = ?</code> so users can&#39;t read each other&#39;s feeds, shares, tags, or saved searches. Article tag endpoints additionally call <code>requireArticleAccess</code>, which confirms the user is subscribed to the article&#39;s feed before allowing tag mutations.</p><h2 id="csrf" tabindex="-1">CSRF <a class="header-anchor" href="#csrf" aria-label="Permalink to &quot;CSRF&quot;">​</a></h2><p>Double-submit pattern. A random <code>ember_csrf</code> cookie (8 random bytes hex-encoded, not HttpOnly) must be echoed in the <code>X-Ember-CSRF</code> header on every state-changing request (<code>POST</code>, <code>PATCH</code>, <code>DELETE</code>).</p><p>Two safe exceptions:</p><ul><li><code>POST /api/auth/login</code> — bootstrap, no session cookie yet.</li><li>Anonymous requests to <code>GET</code> endpoints — no session means nothing to forge.</li></ul><p>The login bypass uses <strong>exact path match</strong>, not suffix match.</p><h2 id="ssrf-protection" tabindex="-1">SSRF protection <a class="header-anchor" href="#ssrf-protection" aria-label="Permalink to &quot;SSRF protection&quot;">​</a></h2><p>Every outbound URL fetch passes through <code>internal/urlcheck.Check</code>:</p><ul><li>Scheme allowlist: <code>http</code>, <code>https</code>. Everything else (<code>file</code>, <code>gopher</code>, <code>javascript</code>) is refused.</li><li>Private-IP block: literal IPs and DNS resolutions inside RFC1918 (<code>10/8</code>, <code>172.16/12</code>, <code>192.168/16</code>), loopback (<code>127/8</code>, <code>::1</code>), link-local (<code>169.254/16</code> — also the AWS / GCP / Azure metadata endpoint — and <code>fe80::/10</code>), CGNAT (<code>100.64/10</code>), unspecified (<code>0.0.0.0/8</code>), and IPv6 ULA (<code>fc00::/7</code>) are refused.</li><li>Redirect chains: feed fetcher rejects 30x to private addresses via <code>feed.RedirectGuard</code>.</li><li>Opt-in bypass: <code>EMBER_ALLOW_PRIVATE_URLS=1</code> skips the IP check for homelabs that need LAN feeds. Scheme allowlist still applies.</li></ul><p>Surfaces covered:</p><ul><li><code>POST /api/feeds</code> (add feed)</li><li><code>POST /api/feeds/import</code> (OPML import — each <code>xmlUrl</code> is filtered)</li><li>Poller readability enrichment (Lobsters / HN aggregator → external link)</li><li>Feed fetcher redirects</li><li><code>POST /api/articles/{id}/extract</code> (on-demand Re-extract) — runs the same <code>urlcheck.Check</code> before fetching the article URL through readability.</li></ul><p>The admin-only <code>POST /api/admin/settings/email-test</code> opens an SMTP TCP connection to the admin-supplied <code>host:port</code>. This is <strong>by design</strong>: the same connection happens every 5 minutes from the digest sender when SMTP is configured. Access is gated by <code>is_admin = 1</code> so a non-admin can&#39;t use it as a port-scan or relay-probe primitive. On failure the endpoint returns a <strong>generic</strong> message; the underlying SMTP/TLS/DNS error (which can carry server banners, internal hostnames, or AUTH fragments) is logged server-side only.</p><h2 id="smtp-transport" tabindex="-1">SMTP transport <a class="header-anchor" href="#smtp-transport" aria-label="Permalink to &quot;SMTP transport&quot;">​</a></h2><p>Outbound mail (digests + the test message) never sends credentials or message bodies in cleartext across the network:</p><ul><li><code>EMBER_SMTP_STARTTLS=1</code> (default) <strong>requires</strong> STARTTLS — if the server doesn&#39;t advertise it (or a MitM strips it from the EHLO response), the send fails rather than downgrading to plaintext. The TLS handshake pins <code>MinVersion: TLS 1.2</code> and verifies the server certificate.</li><li><code>EMBER_SMTP_STARTTLS=0</code> (plain SMTP) is permitted <strong>only</strong> to a loopback host (<code>localhost</code> / <code>127.0.0.1</code> / <code>::1</code>) — a local relay or sidecar. Plain SMTP to any remote host is refused before the connection opens.</li></ul><h2 id="body-limits" tabindex="-1">Body limits <a class="header-anchor" href="#body-limits" aria-label="Permalink to &quot;Body limits&quot;">​</a></h2><ul><li><code>decodeJSON</code> wraps the body in <code>http.MaxBytesReader</code> capped at <strong>1 MiB</strong>.</li><li>OPML import body capped at <strong>8 MiB</strong> (and the parser reads at most 10 MiB).</li><li>Fever (<code>/fever</code>) form body capped at <strong>64 KiB</strong>.</li><li>Request headers capped at <strong>64 KiB</strong> (<code>http.Server.MaxHeaderBytes</code>).</li><li><code>/api/articles/read</code> (and other bulk endpoints) accept at most <strong>1000</strong> ids per request; <code>/api/articles?limit=</code> is clamped to <strong>200</strong>.</li></ul><h2 id="error-responses" tabindex="-1">Error responses <a class="header-anchor" href="#error-responses" aria-label="Permalink to &quot;Error responses&quot;">​</a></h2><p>5xx responses always read <code>{&quot;error&quot;: {&quot;code&quot;: &quot;internal&quot;, &quot;message&quot;: &quot;internal error&quot;}}</code>. The actual error is logged server-side via <code>slog.Default().Error(...)</code>. No SQLite errors, file paths, or constraint details leak to clients.</p><p>Ollama upstream errors (502 from <code>/api/admin/llm/pull</code> etc.) return generic &quot;Ollama refused the pull&quot; messages; the upstream text goes to logs.</p><h2 id="transport-proxy-expectations" tabindex="-1">Transport / proxy expectations <a class="header-anchor" href="#transport-proxy-expectations" aria-label="Permalink to &quot;Transport / proxy expectations&quot;">​</a></h2><p>Ember serves <strong>plain HTTP only</strong> — it has no TLS listener. The supported deployment terminates TLS in a fronting proxy (Caddy, in the bundled compose) and forwards to <code>:8080</code>. Ember&#39;s <code>:8080</code> should never be reachable directly from the internet.</p><ul><li><strong>Trusted proxies</strong>: <code>X-Real-IP</code> (rate-limit keying) and <code>X-Forwarded-Proto</code> (HTTPS detection) are honored <strong>only</strong> from peers in <code>EMBER_TRUSTED_PROXIES</code>. The default is <strong>empty — trust nobody</strong>: Ember treats itself as the edge and reads the real client from the connection, ignoring those headers. Set <code>EMBER_TRUSTED_PROXIES</code> to your proxy&#39;s address/range (the bundled compose sets it to the Caddy bridge range). This closes the spoofing gap where a same-LAN or same-Docker-network peer could forge <code>X-Real-IP</code> to bypass the limiter or poison logs.</li><li><strong>HSTS</strong> is emitted <strong>only over HTTPS</strong> — when the request arrives over TLS, or a trusted proxy reports <code>X-Forwarded-Proto: https</code>. Browsers ignore HSTS received over plain HTTP (RFC 6797), so it is never sent on a bare-HTTP connection.</li><li><strong>Cookies</strong> carry <code>Secure</code> by default. Behind a TLS proxy that&#39;s correct. For a deliberate plain-HTTP deployment (e.g. private VPN) set <code>EMBER_SECURE_COOKIES=false</code>, otherwise browsers drop the cookie over HTTP and login silently fails. Ember logs a startup warning when it&#39;s bound to a non-loopback address with <code>Secure</code> cookies on and no trusted proxy configured.</li><li><code>Permissions-Policy</code>, <code>X-Frame-Options: DENY</code>, <code>X-Content-Type-Options: nosniff</code>, COOP, CORP, and a locked-down CSP (<code>default-src &#39;self&#39;; object-src &#39;none&#39;; base-uri &#39;self&#39;; form-action &#39;self&#39;; frame-ancestors &#39;none&#39;</code>) are set on every response, including 404/405 and error responses. The CSP intentionally allows <code>img-src https:</code> (third-party feed images) and <code>style-src &#39;unsafe-inline&#39;</code> (the SPA&#39;s scoped styles) — accepted trade-offs for a self-hosted reader.</li></ul><h2 id="database" tabindex="-1">Database <a class="header-anchor" href="#database" aria-label="Permalink to &quot;Database&quot;">​</a></h2><ul><li>SQLite WAL with single-writer semantics. <code>MaxOpenConns=1</code> to avoid <code>SQLITE_BUSY</code> storms.</li><li><code>synchronous=NORMAL</code> (safe with WAL), <code>busy_timeout=5s</code>, 64 MiB cache, 256 MiB mmap.</li><li>Backups via <code>VACUUM INTO</code> are safe to run live and produce a compacted snapshot.</li></ul><h2 id="secrets-at-rest" tabindex="-1">Secrets at rest <a class="header-anchor" href="#secrets-at-rest" aria-label="Permalink to &quot;Secrets at rest&quot;">​</a></h2><p>Admin-editable secrets — currently the SMTP password (<code>smtp_password</code> key in <code>app_settings</code>) — are stored <strong>as plaintext</strong> in the SQLite database. This matches the storage model when the same value is supplied via <code>EMBER_SMTP_PASSWORD</code> (env vars are also plaintext, just in <code>.env</code> rather than <code>ember.db</code>).</p><p>Protect the SQLite file at the filesystem layer:</p><ul><li>Docker compose mounts <code>ember-data:/data</code> (root-owned inside the container).</li><li>Backups produced by <code>/api/admin/db/backup</code> inherit those permissions. Don&#39;t ship them to anywhere less trustworthy than the host.</li><li>Database-encryption-at-rest (SQLCipher) is not currently wired; if you need it, a future change would belong here.</li></ul><h2 id="fever-shim" tabindex="-1">Fever shim <a class="header-anchor" href="#fever-shim" aria-label="Permalink to &quot;Fever shim&quot;">​</a></h2><p>The Fever-compatible endpoint (<code>/fever</code>) uses a per-user random 32-byte token stored in the <code>fever_token</code> column. Constant-time compare in the auth path. Lazy-backfilled on first <code>/api/me</code> hit for users created before the column existed. The token is shown to the owning user only via <code>/api/me</code>; admin user lists never include it.</p><h2 id="cve-posture" tabindex="-1">CVE posture <a class="header-anchor" href="#cve-posture" aria-label="Permalink to &quot;CVE posture&quot;">​</a></h2><ul><li>Go stdlib pinned to <strong>1.26.3</strong>.</li><li>CI runs <code>go vet</code> and <code>govulncheck</code> on every push.</li><li>Dependabot opens PRs weekly for <code>gomod</code> + <code>npm</code> updates.</li></ul>',40)])])}const m=o(a,[["render",i]]);export{u as __pageData,m as default};
