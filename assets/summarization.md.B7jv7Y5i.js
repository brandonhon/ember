import{c as t,Q as a,j as s,m as r}from"./chunks/framework.BPKcPtvA.js";const u=JSON.parse('{"title":"Summarization","description":"","frontmatter":{},"headers":[],"relativePath":"summarization.md","filePath":"summarization.md","lastUpdated":1789499754000}'),o={name:"summarization.md"};function i(d,e,n,l,h,c){return a(),s("div",null,[...e[0]||(e[0]=[r('<h1 id="summarization" tabindex="-1">Summarization <a class="header-anchor" href="#summarization" aria-label="Permalink to &quot;Summarization&quot;">​</a></h1><p>Ember can generate a short AI summary for every article. By default it uses a local Ollama model and nothing leaves your server; it can also talk to any OpenAI-compatible endpoint or to Claude — see <a href="#backends">Backends</a>. This page covers how an article gets a summary, the <code>summary_model</code> state that tracks where each article is in that process, and how to recover a backlog that&#39;s stuck.</p><h2 id="how-an-article-gets-a-summary" tabindex="-1">How an article gets a summary <a class="header-anchor" href="#how-an-article-gets-a-summary" aria-label="Permalink to &quot;How an article gets a summary&quot;">​</a></h2><ol><li>The poller ingests an article. It&#39;s inserted with <code>summary_model</code> <code>NULL</code> — pending.</li><li>The article is queued for the summarizer. A pending article is held back from every list and count (the &quot;summary gate&quot;) until either the summarizer finishes it, or the <strong>summary grace window</strong> (<code>EMBER_SUMMARY_GRACE_SECONDS</code>, default 120s, runtime-tunable in <strong>Settings → Language model → Article visibility</strong>) lapses — whichever comes first. That window exists so a slow model delays an article rather than hiding it indefinitely.</li><li>The summarizer calls the backend. On success, the text is stored and <code>summary_model</code> is set to the model name. On failure — backend down, empty output, a persist error, or the request exceeding <code>EMBER_SUMMARY_TIMEOUT_SECONDS</code> — Ember writes <code>summary_model = &#39;skipped&#39;</code> rather than leaving the row pending, so the article still surfaces.</li></ol><p><code>summary_timeout_seconds</code> accepts 10–900, but on the <strong>Claude</strong> backend only 10–600 is reachable: the Anthropic SDK&#39;s non-streaming timeout calculation returns a flat 10-minute default for Ember&#39;s <code>MaxTokens</code> setting, and that applies whenever it lands before the caller&#39;s context deadline — so a value of 601–900 is silently clamped to 600 on Claude. Ollama and the OpenAI-compatible backend have no client-level timeout and honour the full range.</p><h2 id="backends" tabindex="-1">Backends <a class="header-anchor" href="#backends" aria-label="Permalink to &quot;Backends&quot;">​</a></h2><p>Which transport does the work is chosen in <strong>Settings → Language model → Backend</strong> and persisted, so it survives a restart and applies to the next article without one. The <code>EMBER_SUMMARY_*</code> env vars set the boot-time default; the admin&#39;s choice wins from then on.</p><table tabindex="0"><thead><tr><th>Backend</th><th>Talks to</th><th>Needs</th><th>Article text leaves the host?</th></tr></thead><tbody><tr><td><strong>Ollama</strong> (default)</td><td>Ollama&#39;s native <code>/api/generate</code></td><td><code>EMBER_OLLAMA_URL</code> + a pulled model</td><td>No</td></tr><tr><td><strong>OpenAI-compatible</strong></td><td><code>/v1/chat/completions</code> on any compatible endpoint: OpenAI, OpenRouter, Groq, Mistral, Gemini&#39;s compatibility endpoint, vLLM, llama.cpp, LiteLLM</td><td>a base URL <strong>and</strong> a model id; an API key only if the server wants one</td><td>Yes, to that endpoint</td></tr><tr><td><strong>Claude</strong></td><td>Anthropic&#39;s Messages API</td><td>an API key</td><td>Yes, to Anthropic</td></tr></tbody></table><p>All three run the same prompt and the same parser, so summaries look the same whichever you pick. What differs is what the backend can do besides summarize:</p><ul><li><strong>Model pull, delete, the installed-model list, the active-model picker, the host recommendation and the temperature / Top P / context-window tuning are Ollama-only.</strong> None of them mean anything for a hosted provider — there is no local cache to manage — so those endpoints answer <code>503 not_ollama</code> and the UI hides the cards.</li><li>The API key is stored write-only. The server reports whether a key exists (<code>api_key_set</code>), never the value, and an empty key on save means &quot;keep the stored one&quot;. Use <strong>Clear stored key</strong> to erase it.</li></ul><p>A backend that cannot answer is refused at save time (<code>openai</code> with no base URL, <code>anthropic</code> with no API key, a base URL that isn&#39;t <code>http</code>/<code>https</code> → <code>400</code>). A backend that is selected but not yet configured leaves summarization off: incoming articles are stamped <code>disabled</code> — visible straight away, and reversible with <strong>Requeue drained articles</strong> once the configuration is finished — rather than <code>skipped</code>, which is terminal.</p><h2 id="choosing-what-gets-summarized" tabindex="-1">Choosing what gets summarized <a class="header-anchor" href="#choosing-what-gets-summarized" aria-label="Permalink to &quot;Choosing what gets summarized&quot;">​</a></h2><p>Summarizing everything is the right default only for a feed you read end to end. Most feeds are mixed: you skim most of the articles and read the rest, and which is which is only clear once you&#39;ve seen the title. <strong>On-demand mode</strong> waits for you to say so — an article arrives readable, with no summary, and gets one the moment you star it, save it for later, or pin it to a board. Those are the same three signals that already keep an article past the retention window, so there is nothing new to learn.</p><p>Three controls set it, at three scopes:</p><ul><li><strong>Server-wide</strong> — <strong>Settings → Language model → Summaries → Summarize</strong>: <em>Every article</em> or <em>When I mark it</em>. This is the default every feed falls back to.</li><li><strong>Per feed</strong> — the feed&#39;s <strong>⋯</strong> menu in the sidebar: <em>Summaries: default</em> (follow the server), <em>every article</em>, <em>when I mark one</em>, or <em>never</em>.</li><li><strong>Per board</strong> — the ✨ control on a board row: whether pinning an article there counts as &quot;I mean to read this&quot;. A board you file things in rather than read from should turn it off.</li></ul><h3 id="resolution-order" tabindex="-1">Resolution order <a class="header-anchor" href="#resolution-order" aria-label="Permalink to &quot;Resolution order&quot;">​</a></h3><p>For one article, in order — the first rule that applies decides:</p><ol><li><strong>Per-feed &quot;never&quot;</strong> wins outright. If every subscriber of the feed has turned summaries off for it, the article is stamped <code>&#39;excluded&#39;</code> and nothing else is consulted. It is an opt-<em>out</em>, and no mode overrides it.</li><li><strong>The effective feed mode.</strong> A feed&#39;s subscribers vote, and <strong>any subscriber wanting every article wins</strong>: if even one of them has chosen <em>every article</em> — explicitly, or by inheriting a server default of <em>every article</em> — the feed summarizes everything. This is not a tie-break, it&#39;s the only safe rule: the summary lives on the shared article row, so one reader choosing on-demand must not take away a summary another reader asked for.</li><li><strong>The server-wide mode</strong>, for any subscriber with no opinion of their own.</li></ol><p>An article that on-demand mode declines to queue is stamped <code>&#39;deferred&#39;</code>, which satisfies the summary gate — so it is readable straight away rather than hidden waiting for a summary that isn&#39;t coming.</p><h3 id="a-worked-example" tabindex="-1">A worked example <a class="header-anchor" href="#a-worked-example" aria-label="Permalink to &quot;A worked example&quot;">​</a></h3><p>A shared feed, three subscribers, server-wide mode <em>When I mark it</em>:</p><table tabindex="0"><thead><tr><th>Subscriber</th><th>Their setting</th><th>Wants every article?</th></tr></thead><tbody><tr><td>Ana</td><td><em>Summaries: default</em></td><td>No — inherits <em>When I mark it</em></td></tr><tr><td>Ben</td><td><em>Summaries: every article</em></td><td><strong>Yes</strong></td></tr><tr><td>Cara</td><td><em>Summaries: never</em></td><td>No — opted out</td></tr></tbody></table><p>Ben&#39;s choice decides it: the feed summarizes every article as it arrives. Cara still sees no summaries — her opt-out is per account and blanks the text for her — and Ana gets the summaries for free, because they were generated anyway. Had Ben switched to <em>default</em> or <em>when I mark one</em>, the feed would fall to on-demand and new articles would arrive <code>&#39;deferred&#39;</code> until someone starred, saved, or pinned one.</p><p>Switching <strong>to</strong> <em>every article</em> backfills, at either scope: the articles already stamped <code>&#39;deferred&#39;</code> are cleared and re-queued, so the choice acts on the backlog and not only on what gets published next. A feed-level switch backfills that feed; the server-wide switch backfills everything still deferred. Switching the other way — to <em>when I mark one</em> — deliberately un-summarizes nothing and enqueues nothing; it only changes what happens to the next batch.</p><h2 id="the-summary-model-state-table" tabindex="-1">The <code>summary_model</code> state table <a class="header-anchor" href="#the-summary-model-state-table" aria-label="Permalink to &quot;The `summary_model` state table&quot;">​</a></h2><p><code>summary_model</code> is the whole state machine. The <code>summary</code> text column holds output only — it is <strong>never</strong> the state, and an empty summary is not by itself terminal. That distinction is exactly what made issue <a href="https://github.com/brandonhon/ember/issues/198" target="_blank" rel="noreferrer">#198</a> hard to diagnose from the database: the intuitive fix, blanking <code>summary</code>, does nothing, because nothing reads <code>summary</code> to decide whether an article is done.</p><table tabindex="0"><thead><tr><th><code>summary_model</code></th><th>Meaning</th><th>Visible in lists?</th><th>Counted in <code>pending_summary</code>?</th></tr></thead><tbody><tr><td><code>NULL</code> or <code>&#39;&#39;</code></td><td>Pending — the summarizer hasn&#39;t finished with it.</td><td>Only after the grace window</td><td>Yes</td></tr><tr><td><code>&#39;&lt;model name&gt;&#39;</code></td><td>Summarized successfully; <code>summary</code> holds the text.</td><td>Yes</td><td>No</td></tr><tr><td><code>&#39;skipped&#39;</code></td><td>Attempted and failed (backend down, empty output, persist error, timeout).</td><td>Yes</td><td>No</td></tr><tr><td><code>&#39;excluded&#39;</code></td><td>Every subscriber of the feed opted out of summaries.</td><td>Yes</td><td>No</td></tr><tr><td><code>&#39;disabled&#39;</code></td><td>Finalized while summaries were switched off. Also cleared, alongside every other non-<code>&#39;excluded&#39;</code> state, by <strong>Resummarize all</strong>; the poller re-stamps <code>&#39;disabled&#39;</code> the next time it lands if summaries are still off.</td><td>Yes</td><td>No</td></tr><tr><td><code>&#39;deferred&#39;</code></td><td>On-demand mode: not queued until a reader stars, saves, or pins it. Cleared by any of those three, by switching the feed to <em>every article</em>, and by <strong>Resummarize all</strong>, which re-defers it on the next poller tick if the feed is still on-demand.</td><td>Yes</td><td>No</td></tr></tbody></table><p>Every non-empty value satisfies the summary gate — that&#39;s why every give-up path writes a terminal marker instead of leaving the row <code>NULL</code>.</p><p>To see the real distribution in your database:</p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SELECT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> IFNULL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(summary_model,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;(pending)&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">AS</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> state</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">COUNT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">FROM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> articles </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">GROUP BY</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ORDER BY</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> DESC</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><h2 id="runbook-summarizing-n-articles-never-goes-down" tabindex="-1">Runbook: &quot;Summarizing N articles…&quot; never goes down <a class="header-anchor" href="#runbook-summarizing-n-articles-never-goes-down" aria-label="Permalink to &quot;Runbook: &quot;Summarizing N articles…&quot; never goes down&quot;">​</a></h2><p>This is issue #198. Some articles never got finalized — most commonly a restart landing between an article&#39;s insert and the poller&#39;s stamp — so they sit <code>NULL</code> forever, pinned in the pending count and hidden behind the summary gate.</p><p><strong>First move:</strong> <strong>Settings → Language model → Summarization queue → Drain queue</strong> (admin only). This stamps every pending article <code>&#39;disabled&#39;</code>, which makes it visible immediately and drops it out of the pending count. No text is lost — a pending article has no summary yet by definition — but those articles won&#39;t be summarized until you requeue them.</p><p>To send drained articles back through the summarizer, use <strong>Requeue</strong> in the same panel. It clears only the <code>&#39;disabled&#39;</code> marker; articles that genuinely failed (<code>&#39;skipped&#39;</code>) or were opted out per-feed (<code>&#39;excluded&#39;</code>) are left alone on purpose — use <strong>Resummarize</strong> (per-feed, or <strong>Resummarize all</strong> for the whole database) for those instead. Resummarize all clears every non-<code>&#39;excluded&#39;</code> state, including <code>&#39;deferred&#39;</code> and <code>&#39;disabled&#39;</code>, not just <code>&#39;skipped&#39;</code> and finished summaries — it converges harmlessly, since the poller and the disabled-switch path both re-stamp those markers on their next pass, but it does mean a Resummarize-all run also re-processes articles that were merely deferred or drained, not only failures.</p><p>If you don&#39;t have shell access to the admin UI, the same actions are:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>POST /api/admin/summaries/drain    → {&quot;drained&quot;: N}</span></span>\n<span class="line"><span>POST /api/admin/summaries/requeue  → {&quot;reset&quot;: N, &quot;enqueued&quot;: M}</span></span></code></pre></div><p><strong>Do not try <code>UPDATE articles SET summary = &#39;&#39; WHERE summary IS NULL</code>.</strong> That was the hand-written fix the #198 reporter tried first, and it does nothing — <code>summary</code> isn&#39;t the state. The only column that matters is <code>summary_model</code>.</p><h2 id="turning-summaries-off" tabindex="-1">Turning summaries off <a class="header-anchor" href="#turning-summaries-off" aria-label="Permalink to &quot;Turning summaries off&quot;">​</a></h2><p>The global switch (<strong>Settings → Language model</strong>) is a stored setting, not just an env default — it takes effect immediately and survives a restart. Turning it off:</p><ul><li>Stops new articles from being queued for summarization.</li><li><strong>Drains the existing pending queue</strong> — every article still <code>NULL</code>/<code>&#39;&#39;</code> is stamped <code>&#39;disabled&#39;</code> in the same request, so nothing stays stuck &quot;summarizing&quot; while the feature is off.</li></ul><p>Turning it back on <strong>requeues exactly those articles</strong> — the ones stamped <code>&#39;disabled&#39;</code> — without re-running anything that had already succeeded, been skipped, or been excluded.</p><p>Switching the <strong>mode</strong> back to <em>Every article</em> works the same way one marker over: the articles stamped <code>&#39;deferred&#39;</code> are cleared and re-queued, and nothing else is touched. Both switches act on the backlog they created, so neither looks inert until the next poll.</p><h2 id="what-each-control-does" tabindex="-1">What each control does <a class="header-anchor" href="#what-each-control-does" aria-label="Permalink to &quot;What each control does&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Control</th><th>Scope</th><th>Where</th><th>Marker it produces</th></tr></thead><tbody><tr><td>Global summaries switch</td><td>Server-wide</td><td>Settings → Language model</td><td><code>&#39;disabled&#39;</code> on every pending article when turned off</td></tr><tr><td>Per-feed opt-out (&quot;Summaries: never&quot;)</td><td>Per account, per feed</td><td>Feed&#39;s <strong>⋯</strong> menu in the sidebar</td><td><code>&#39;excluded&#39;</code> once every subscriber has opted out</td></tr><tr><td>Summarize: every article / when I mark it</td><td>Server-wide</td><td>Settings → Language model → Summaries</td><td><code>&#39;deferred&#39;</code> on articles nobody has asked for</td></tr><tr><td>Summaries: default / every article / when I mark one</td><td>Per account, per feed</td><td>Feed&#39;s <strong>⋯</strong> menu in the sidebar</td><td><code>&#39;deferred&#39;</code>, unless another subscriber wants every article</td></tr><tr><td>Summarize what I pin here</td><td>Per account, per board</td><td>✨ on the board row in the sidebar</td><td>None — it decides whether pinning <em>clears</em> <code>&#39;deferred&#39;</code></td></tr></tbody></table>',44)])])}const p=t(o,[["render",i]]);export{u as __pageData,p as default};
